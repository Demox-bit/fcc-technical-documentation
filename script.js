
    document.addEventListener('DOMContentLoaded', function() {

        const span = document.getElementById('toggle-hidden');

        

        span.addEventListener('click', function() {

            const hiddenSection = document.getElementById('Hidden_place');


            hiddenSection.style.display = hiddenSection.style.display === 'none' ? 'block' : 'none';

            hiddenSection.scrollIntoView({ behavior: 'smooth' });

        });

    });


    document.addEventListener('DOMContentLoaded', function() {

        const span = document.getElementById('toggle-hidden2');

        

        span.addEventListener('click', function() {

            const hiddenSection = document.getElementById('Hidden_place2');

            hiddenSection.style.display = hiddenSection.style.display === 'none' ? 'block' : 'none';

            hiddenSection.scrollIntoView({ behavior: 'smooth' });

        });

    });

    document.addEventListener('DOMContentLoaded', function() {

        const span = document.getElementById('toggle-hidden3');

        

        span.addEventListener('click', function() {

            const hiddenSection = document.getElementById('Hidden_place2');

            hiddenSection.style.display = hiddenSection.style.display === 'none' ? 'block' : 'none';

            hiddenSection.scrollIntoView({ behavior: 'smooth' });

        });

    });

    document.addEventListener('DOMContentLoaded', function() {

        const span = document.getElementById('toggle-hidden9');

        

        span.addEventListener('click', function() {

            const hiddenSection = document.getElementById('Hidden_place9');

            hiddenSection.style.display = hiddenSection.style.display === 'none' ? 'block' : 'none';

            hiddenSection.scrollIntoView({ behavior: 'smooth' });

        });

    });
